{
  "name": "step-english",
  "version": "1.1.0",
  "private": true,
  "description": "STEP English study site with accounts, leaderboard, challenges and adaptive training",
  "scripts": {
    "dev": "node dev/server.js",
    "deploy": "node tools/bump.js && vercel deploy --prod --yes",
    "bump": "node tools/bump.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
